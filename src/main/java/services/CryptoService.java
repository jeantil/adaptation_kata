package services;

public class CryptoService {

    public String cryptFilename(String filename) {
        // super duper hyper secure encryption scheme should be here
        // the point is to "need" an external service for some conversions
        return filename;
    }

    public String decryptFilename(String filename) {
        //same as for crypt filename
        return filename;
    }
}
